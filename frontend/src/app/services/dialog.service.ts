
class DialogService {
    /*
    constructor(private dialog: MatDialog) {}

    open(component: any, data: any = {}, options: any = {}) {
        return this.dialog.open(DialogComponent(), {data, ...options});
    }*/
}
